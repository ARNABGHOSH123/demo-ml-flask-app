apiVersion: serving.knative.dev/v1
kind: Service
metadata:
  name: ml-sales-service-cloud-run-arnab-ghosh
spec:
  template:
    spec:
      containers:
        - image: my-app-image
          resources:
            limits:
              cpu: 1000m
              memory: 128Mi
